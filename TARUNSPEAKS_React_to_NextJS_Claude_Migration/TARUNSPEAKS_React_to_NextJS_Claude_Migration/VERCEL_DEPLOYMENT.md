# Vercel Deployment

Verify:
`npm run build`
`npm run start`

Audit all environment variables. Only intentionally public variables use `NEXT_PUBLIC_*`. Test a Vercel preview before production. Verify HTTPS, canonical domain, redirects, metadata, sitemap, robots, images, forms, and analytics.
