# SEO Architecture

Technical SEO must be part of the application architecture.

Required layers:
Page Content → Semantic HTML → Metadata → Canonical → Social Metadata → Structured Data → Internal Links → Sitemap → Robots.

Index only genuine public, canonical, useful pages. Use `generateMetadata()` for dynamic routes. Never fabricate SEO claims or keyword-stuffed content. Inspect actual server-rendered HTML during validation.
